<UserControl x:Class="AudibleZenBot.WPF.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="24" FontWeight="Bold" Foreground="#ffffff">Settings</TextBlock>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,6,0,0">
            <StackPanel Orientation="Vertical" Margin="8">
                <GroupBox Header="Debug Logging" Margin="0,0,0,8">
                    <StackPanel Margin="6">
                        <CheckBox x:Name="ChkEnableLogging" Content="Enable debug logging to file" Foreground="White" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="Log Folder:" Foreground="#cccccc" VerticalAlignment="Center" />
                            <TextBox x:Name="TbLogFolder" Width="300" Margin="8,0,0,0" />
                            <Button x:Name="BtnBrowseLog" Content="Browse" Width="80" Margin="8,0,0,0" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Username Colors" Margin="0,0,0,8">
                    <StackPanel Margin="6">
                        <TextBlock Text="Customize username colors (20 colors expected)." Foreground="#cccccc" />
                        <WrapPanel x:Name="ColorsWrap" Margin="0,6,0,0" />
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Ngrok Configuration" Margin="0,0,0,8">
                    <StackPanel Margin="6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Auth Token:" Foreground="#cccccc" VerticalAlignment="Center" />
                            <TextBox x:Name="TbNgrokToken" Width="360" Margin="8,0,0,0" />
                            <Button x:Name="BtnSaveNgrok" Content="Save Token" Width="100" Margin="8,0,0,0" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Platform Credentials" Margin="0,0,0,8">
                    <StackPanel Margin="6">
                        <TextBlock Text="Client ID / Secret entries for platforms." Foreground="#cccccc" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="Twitch Client ID:" Foreground="#cccccc" VerticalAlignment="Center" />
                            <TextBox x:Name="TbTwitchClientId" Width="300" Margin="8,0,0,0" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="About">
                    <StackPanel Margin="6">
                        <TextBlock Text="AudibleZenBot - Settings (stub)" Foreground="#cccccc" />
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        </Grid>
    </UserControl>