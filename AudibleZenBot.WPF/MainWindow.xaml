<Window x:Class="AudibleZenBot.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AudibleZenBot.WPF"
        Title="AudibleZenBot" Height="700" Width="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left navigation -->
        <StackPanel Grid.Column="0" Background="#252525">
            <ListBox x:Name="NavList" Margin="8" Foreground="White" Background="#2b2b2b" SelectedIndex="0">
                <ListBoxItem>Chat Messages</ListBoxItem>
                <ListBoxItem>Platform Connections</ListBoxItem>
                <ListBoxItem>Settings</ListBoxItem>
                <ListBoxItem>Chat Overlay Configuration</ListBoxItem>
                <ListBoxItem>Automation</ListBoxItem>
            </ListBox>
        </StackPanel>

        <!-- Content area -->
        <ContentControl x:Name="ContentArea" Grid.Column="1" />
    </Grid>
</Window>
